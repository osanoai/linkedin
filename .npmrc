# Supply-chain protection: refuse to install any dependency version published
# less than 7 days ago (10080 minutes). Enforced by pnpm >= 10.16, which is the
# pinned package manager for this repo (see "packageManager" in package.json).
minimum-release-age=10080

# Fail installs on Node versions outside the range declared in package.json.
engine-strict=true
